#include <stdio.h>
void main()
{
      char str [81];
      int  nspace;
      int i;
      clrscr();
      printf("Enter a line of text:\n");
      gets(str);
        nspace = 0; 
      i = 0;
      while (str[i] != '\0')
          {
          
               if (ch == ' ' || ch =='\n' || ch == '\t')
                       nspace++;
          
          }
           
              printf("White spaces : %d",  nspace);
        
              getch();
} 
